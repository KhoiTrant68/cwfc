# Core — all mechanism scripts (derisk_cot, g1_pilot, kaggle_g1, g4_wflow,
# wflow_endpoint self-test)
torch
numpy

# viz.py + g4_wflow.py --save_samples (figures from result JSONs / sample grids)
matplotlib

# derisk_mixture_entropy.py — real rANS coder + LIC backbone
compressai
pillow
torchvision

# derisk_dp.py "real" mode — Stable-Diffusion VAE backbone + LPIPS metric
# (the toy/feat modes need neither). Pinned to match real/requirements.txt's
# SD3-track pin (see the comment block there): `import diffusers` transitively
# imports transformers at module-load time (diffusers.loaders.unet), so an
# unpinned diffusers here can pull a release whose import chain breaks
# against whatever transformers happens to be installed, even though this
# script only ever calls AutoencoderKL.from_pretrained. If real/requirements.txt
# is also installed in this environment, transformers==4.44.2 from there
# satisfies this; otherwise install a compatible transformers alongside it.
diffusers==0.31.0      # --backbone vae
lpips          # --percep lpips
